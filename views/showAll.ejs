<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />

    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
      integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk"
      crossorigin="anonymous"
    />

    <title>Movies</title>
  </head>
  <body>
    <div class="container">
      <h1>All Movies</h1>
      <p>
        <a class="btn btn-primary" href="/movies/add" role="button"
          >Add Movie</a
        >
      </p>
      <table class="table">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Title</th>
            <th scope="col">Genres</th>
            <th scope="col">Year</th>
            <th scope="col">Photo</th>
            <th scope="col">Action</th>
          </tr>
        </thead>
        <tbody>
          <% no = 1; %> <% for (let i = 0; i < data.length; i++) { %>
          <tr>
            <th scope="row"><%= no++ %></th>
            <td><%= data[i].title %></td>
            <td><%= data[i].genres %></td>
            <td><%= data[i].year %></td>
            <td>
              <img src="<%= data[i].photo %>" width="100px" alt="photo" />
            </td>
            <td>
              <a
                class="btn btn-warning"
                href="/movies/edit/<%= data[i].id %>"
                role="button"
                >Edit</a
              >
              <a
                class="btn btn-danger"
                href="/movies/delete/<%= data[i].id %>"
                role="button"
                onclick="return confirm('Yakin ingin menghapus?')"
                >Delete</a
              >
            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>
